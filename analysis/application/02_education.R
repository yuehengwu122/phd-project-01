# ============================================================
# Application 2: Education
#
# (Placeholder -- dataset to be determined)
# ============================================================

source("src/_setup.R")

# TODO: load dataset
# X <- ...
# y <- ...

# fit_d0 <- fit_model(X, y, model = "full", d_order = 0,
#                      chains = 2, iter = 5500, warmup = 500)
#
# summary_gp_fit(fit_d0)
# compute_bf_table(fit_d0)
# plot_gp_trends(fit_d0)
